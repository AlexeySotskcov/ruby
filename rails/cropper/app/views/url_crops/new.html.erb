<%= form_for @url do |f| %>

  <%= f.label :base_url %>
  <%= f.text_field :base_url%>

  <%= f.label "Short link" %>
  <%= f.text_field :short_url, readonly: true %>

  <%= f.submit "Crop"%>

  <%= button_tag "Reset", type: :reset %>

<% end %>
